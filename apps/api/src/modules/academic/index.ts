// Public API of the `academic` module.
//
// Other modules may import ONLY from this file — never from commands/,
// queries/, the repository, or internal types. ESLint enforces that.
//
// Export the smallest surface that lets another module do its job: a command
// it may invoke, a query it may run, and the types those need. Everything
// else stays private, so the module can be reorganised without touching
// callers.
//
// Empty until this module has something worth exposing.

export {};
