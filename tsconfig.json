{
  "$comment": [
    "Solution config. It compiles nothing itself — it points editors and `tsc -b` at the real",
    "projects, so opening the repo root type-checks every workspace with its own settings.",
    "Shared compiler options live in tsconfig.base.json, which each project extends."
  ],
  "files": [],
  "references": [
    { "path": "./apps/api" },
    { "path": "./apps/web" },
    { "path": "./packages/contracts" }
  ]
}
